[project]
name = "psidautoprepare"
version = "0.1.0"
description = "Prepare images for ID card creation."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.1",
    "opencv-python>=5.0.0.93",
    "pillow>=12.3.0",
    "tqdm>=4.68.3",
]
